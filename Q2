
#include <stdio.h>
#include <string.h>

int findChar(char * str, char c)
{
  int i,l, count;
  l = strlen(str);
    for (i=0;i<l;i++)
       {
       if (str[i] == c)
         return i;
       }  
       
         return -1;           

 }

int replaceChar(char * str, char c1, char c2);   
{
  
}




int main()
{
char arv[10] = "count";


printf("Find in position %d",findChar(arv,'c'));

return 0;


}   
