/*#include <stdio.h>
#include <string.h>





int main (void) 
{
   char s1[15] ;
   char s2[15] ;

   char temp;

   int i, j, ch;
   int n  = strlen(s1);
   int n1 = strlen(s2);
   printf("Please enter first word: ");
   scanf ("%s",s1);

   printf("please enter the second word: ");
   scanf("%s",s2);
   // If both strings are of different length, then they are not anagrams

   if( n != n1) {    
      printf(" Sorry! %s and %s are not anagrams! \n", s1, s2);
      return 0;
   }
   else
   {
  
       for(i=1;i<n;i++)
       {
          for(j=0;j<n-i;j++)
          {
	          if(s1[j]>s1[j+1])
	            {
	             ch=s1[j];
	             s1[j] = s1[j+1];
	             s1[j+1]=ch;
	            }
              
         }
       } 
      printf("After sorting the string appears like : \n");
      printf("%s\n\n",s1);

         for(i=1;i<n1;i++)
       {
          for(j=0;j<n-i;j++)
          {
	          if(s2[j]>s2[j+1])
	            {
	             ch=s2[j];
	             s2[j] = s2[j+1];
	             s2[j+1]=ch;
	            }
          }   
       }
      printf("After sorting the string appears like : \n");
      printf("%s\n\n",s1);
      printf("%s\n\n",s2);

       if (strncmp(s1, s2,15) > 0)
           printf("Strings are anagrams!\n"); 
   
        
      else
         printf("Strings are not anagrams! \n");
         return 0;
      }
}*/
#include <stdio.h>
#include <string.h>

void sortArray(char* ar, int size)
{
  int i,j,ch;
   for(i=1;i<size;i++)
    for(j=0;j<size-i;j++)
	    if(ar[j]>ar[j+1])
	    {
	  ch=ar[j];
	  ar[j] = ar[j+1];
	  ar[j+1]=ch;
	    }

}
int main()
{
  char str[100];
  char oldStr[100];
  char oldStr1[100];
  char str1[100];
  int i,j,l,m,k; 
       printf("Please enter first word: ");
       scanf ("%s",str);
       strncpy(oldStr,str,100);
       printf("Please enter the second word: ");
       scanf ("%s",str1);
       strncpy(oldStr1,str1,100);
     
  l=strlen(str);
  m=strlen(str1);
  if (l == m)
    {
      sortArray(str, l); /* sorting process */
      sortArray(str1, m);

     k = strncmp(str,str1,100);
        if (k == 0)
           printf("Yes! \"%s\" and \"%s\" are anagrams. ", oldStr,oldStr1);
        else
           printf("Sorry! \"%s\" and \"%s\" are not anagrams.",oldStr,oldStr1);
 

    }
   else 
   printf("Sorry! \"%s\" and \"%s\" are not anagrams.",oldStr,oldStr1);
  
   
  }

  
  




  
  



